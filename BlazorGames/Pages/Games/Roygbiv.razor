@page "/roygbiv"
@using Models.Roygbiv

<PageTitle>ROYGBIV</PageTitle>

<h1>ROYGBIV</h1>

@foreach (Card card in Hand)
{
	<p>Card: @card.Value</p>
}

@code {
	List<Card> Hand = new()
	{
		new Card(1),
		new Card(2),
		new Card(3),
	};
}
